import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'rn-lib-template',
  favicon: './logo.png',
  logo: './logo.png',
  outputPath: 'docs-dist',
  mode: 'site',
  // more config: https://d.umijs.org/config
});
